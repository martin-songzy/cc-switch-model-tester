# cc-switch 模型可用性测试工具
（先搜索github上有无类似项目，如有可以基于开源项目进行二次开发）
## 功能要求
从cc-switch(https://github.com/farion1231/cc-switch)的配置文件中读取当前的模型供应商，可用模型。然后对模型可用性进行快速测试。
1. 工具读取cc-switch中claude code，codex和Pi agent 对应的模型供应商配置，让用户自定义测试对象（每个编程工具对应一个独立标签页），能够基于模型供应商，模型，上游格式（chat/respone/message/...）进行关键字过滤，多选项勾选。比如测试某一个或者多个供应商下的所有模型，或者基于某一个模型进行所有站点的测试。
   - 同一测试批次中可能存在重复供应商或重复测试目标；开始测试前必须自动识别并去重，提示重复来源和预计减少的请求数量。只允许对端点、模型、协议、测试模式、认证和有效请求配置均相同的目标去重，不得仅按供应商名称或 ID 去重。
2. 测试完全基于cc-switch的配置，包括上游格式，User-agent等设置
3. 有供应商明确禁止测活，也就是发送“你好/hi/hello”之类的提示词，必须构建一些正常的提示词，然后随机发送，避免封号。提示词预设在工具内，可自定义。
4. 测试次数可自定义，默认是3次。可以设置并发数，全站并发默认10，单站并发默认为1。
5. 可配置代理，默认是socks5://127.0.0.1:1080
6. 测试结果可以筛选，记录测试时间，发送的提示词，返回的结果，按表头字段排序。
7. 返回200不代表模型可用，要设置一些负面词，比如返回是200，但内容是api erro，则判断为模型不可用

## 运行环境
windows环境

## 非功能需求
- 资源占用最小化，尽可能节省CPU/内存

## 编程语言
- 建议用rust语言
